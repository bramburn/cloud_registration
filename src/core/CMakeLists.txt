# Core module CMakeLists.txt
# Reserved for platform-agnostic business logic, data models, and algorithms

# Placeholder for future core library
# add_library(PointCloudCore STATIC)
# target_sources(PointCloudCore PRIVATE)
# target_link_libraries(PointCloudCore Qt6::Core)
