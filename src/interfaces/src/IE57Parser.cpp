#include "interfaces/IE57Parser.h"

// This file is required for Qt's MOC (Meta-Object Compiler) to generate
// the necessary meta-object code for the IE57Parser interface.
// Since IE57Parser contains Q_OBJECT and signals, it needs a corresponding
// .cpp file for the MOC system to work properly.

// The implementation is minimal since IE57Parser is a pure interface,
// but this file ensures that the Qt meta-object system can generate
// the necessary signal/slot infrastructure.

// All actual implementation is in the derived classes like E57ParserLib.
